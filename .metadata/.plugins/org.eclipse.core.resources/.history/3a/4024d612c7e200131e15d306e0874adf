package com.SSS.engine;
import java.nio.FloatBuffer;

import org.lwjgl.BufferUtils;

public class Util {
	public static FloatBuffer createFloaatBuffer(int size) {
		return BufferUtils.createFloatBuffer(size);
	}
	public static void createFlippedBuffer(Vertex[] vertices) {
		FloatBuffer buffer;
		
	}
}